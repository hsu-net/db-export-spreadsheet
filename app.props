<Project>
  <PropertyGroup>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
    <OutputPath>$(MSBuildThisFileDirectory)output\apps\$(AssemblyName)</OutputPath>
  </PropertyGroup>
</Project>